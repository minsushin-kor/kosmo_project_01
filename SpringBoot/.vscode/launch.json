{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "java",
            "name": "Spring Boot - AppApplication",
            "request": "launch",
            "mainClass": "com.car.app.AppApplication",
            "projectName": "app",
            "envFile": "${workspaceFolder}/app/.env"
        }
    ]
}